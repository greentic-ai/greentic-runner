// placeholder — filled in subsequent tasks

/// Tenant identity context threaded through every runtime call (Task 1.2).
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct TenantContext;
